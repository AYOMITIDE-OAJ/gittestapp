import styled from 'styled-components';

export const GifContainerDiv = styled.div`
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat( auto-fit, minimax(250px, 1fr));
    grid-gap: 20px;
    `;

    